services:
  - type: worker
    name: xshot-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python xshot_scanner.py
    plan: free
    envVars:
      - key: TG_TOKEN
        sync: false
      - key: TG_CHAT_ID
        sync: false
